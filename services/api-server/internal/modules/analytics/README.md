# analytics

当前已落地：

- 审计日志列表接口
- 基础运营看板接口
- 基于统一 `audit` 存储的内存实现
- 已接入登录、教务绑定、门户公告发布、站内通知发布/已读、校园生活内容发布、审核更新等关键成功动作

约束：

- 当前分析能力只做后端事实汇总，不由前端自行拼接审计语义
- 管理读取权限以后端 `analytics:view` 为准
- 审计记录默认采用 best effort 写入，不阻塞主业务成功返回

当前边界：

- 当前为 `memory` 审计存储，重启服务后不会保留历史日志
- 暂未接入更完整的埋点、指标、追踪与持久化看板
