class AppConstants {
  const AppConstants._();

  static const appName = 'weouc 校园服务';
  static const apiBaseUrl = 'http://localhost:8080/api';
  static const networkTimeout = Duration(seconds: 12);
}
