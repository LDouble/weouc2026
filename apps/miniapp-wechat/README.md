# 微信小程序

## 技术选型

- 微信原生小程序
- JavaScript
- 原生组件与分包

## 目标

承载高频校园入口、微信生态传播与校园生活主线能力，当前以跑腿、组局（含拼车等轻社交撮合）、二手交易、资料、失物招领为主，后续再承接教务功能。

## 推荐结构

```text
.
├── api/
├── services/
├── stores/
├── pages/
├── components/
├── behaviors/
├── utils/
└── subpackages/
```

## 约束

- 不使用跨端框架
- 默认使用微信小程序登录
- 主包控制体积
- 服务层统一处理鉴权与错误映射
- 未绑定教务的账号不能查看联系方式，页面只按服务端返回结果渲染

## 分层职责

- `api/`：底层传输封装与接口模块，只描述路径、参数、鉴权头和返回包结构。
- `services/`：按业务域组织用例，负责 DTO 到页面语义的映射，不把接口字段拼装散落到页面。
- `stores/`：存放运行期配置与会话状态，只维护客户端必要状态，不承载业务规则。
- `pages/`：只负责交互编排与局部 `setData`，不直接推断绑定状态、联系方式可见性等规则。

## 当前已对齐的核心流

- 微信登录与会话恢复
- 我的页与教务绑定状态展示
- 首页动态流
- 二手、跑腿、资料、失物招领列表页
- 首页门户公告、消息中心与未读通知状态

## 接口文档

- [API.md](/Users/liangluo/code/weouc2026/apps/miniapp-wechat/API.md)：微信小程序当前依赖的后端接口说明


## 提审前功能闭环

- 首页从后端门户接口读取轮播与公告，顶部通知入口直达消息中心。
- 消息中心已接入站内通知、未读数、已读回执和校园公告详情。
- 发布入口覆盖闲置、跑腿、组局、资料、失物招领、拼车；新发布内容按后端审核状态回流到详情或列表。
- 联系方式展示继续只消费服务端裁剪后的字段，未绑定教务时由详情页给出绑定引导。
