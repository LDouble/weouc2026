# 可靠性规范

## 可观测性

- 所有请求生成 `trace_id`
- 日志统一结构化输出
- 核心路径打点：登录、首页、课程查询、活动报名、工单提交

## 稳定性策略

- 接口超时、重试、熔断策略统一收口
- 外部系统调用必须区分“硬失败”和“软降级”
- 定时同步任务必须支持重跑

## 数据安全

- PostgreSQL 定期备份
- 关键业务表支持恢复演练
- 删除操作优先软删除

## 发布策略

- 管理后台和后端支持灰度
- 小程序通过版本审核与回滚策略控制
- Flutter App 发布需保留兼容旧接口周期

