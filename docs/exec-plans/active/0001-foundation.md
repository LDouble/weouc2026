# 0001 基础工程与架构落地计划

## 目标

把仓库从“空目录”推进到“可持续协作的工程底座”。

## 工作流

### 工作流 A：后端基础骨架

- 初始化 `services/api-server`
- 建立模块目录和分层约束
- 接入配置、日志、错误码、鉴权中间件

### 工作流 B：契约与代码生成

- 建立 `packages/contracts/openapi`
- 约定错误码、分页、鉴权、上传等通用 schema
- 设计 JS SDK 与 Dart SDK 生成流程

### 工作流 C：管理员后台外壳

- 初始化 `Vue 3 + Vite + TypeScript`
- 建立路由、布局、权限、SDK 调用骨架

### 工作流 D：微信小程序外壳

- 初始化原生小程序工程
- 建立微信登录、请求、分包、公共组件基础能力
- 建立教务绑定状态感知与联系方式可见性控制的基础能力

当前进展（`2026-05-09`）：

- 已把核心目录职责收敛为 `api/` 传输层、`services/` 业务语义层、`stores/` 会话与配置层、`pages/` 展示层。
- 已重构微信登录、个人中心、教务绑定、首页动态、二手/跑腿/资料/失物招领列表页的数据流。
- 已补充小程序依赖接口文档，后续需继续与 `packages/contracts` 中的正式契约对齐。

当前进展（`2026-05-10`）：

- 已初始化 `services/api-server`，建立 `Go + Gin` 模块化单体基础骨架。
- 已落地配置、结构化日志、请求 ID、统一错误响应、鉴权上下文中间件。
- 已提供 `/healthz`、`/readyz`、`/api/v1/system/profile` 基础接口，并补齐最小 OpenAPI 契约基线。
- 已落地小程序主链路核心接口：微信登录、教务绑定、当前资料、首页动态，以及二手/跑腿/资料/失物招领基础读取和关键交互接口。
- 已接入 `wechat_provider`、`academic_provider` 的 mock 实现，便于无外部依赖条件下联调。
- 已接入腾讯 COS 文件管理，上传链路改为“只存对象路径、读取时签 URL”。

当前进展（`2026-05-11`）：

- 已补齐 `portal` 模块最小能力，支持门户首页聚合、公告列表/详情，以及基于 `portal:publish` 的管理端公告发布。
- 已补齐 `notification` 模块最小能力，支持当前用户通知列表、未读数、已读回执，以及基于 `notification:publish` 的管理端通知发布。
- `M1` 所需的“门户、通知通路可用”已具备后端联调基础，当前仍待后续前端消费与持久化演进。

当前进展（`2026-05-11`）：

- 已补齐 `analytics` 模块最小能力，支持审计日志列表与基础运营看板。
- 已为登录、教务绑定、门户公告发布、站内通知发布/已读、校园生活内容发布、审核更新接入统一审计记录。
- 关键动作已具备“发生即留痕、后台可查”的后端联调基础，后续再演进持久化与更完整埋点。

当前进展（`2026-05-11`）：

- 已补齐 `academic` 模块最小只读能力，支持已绑定学生查询学期、课程表、考试安排、成绩单。
- 教务查询当前统一经 `academic_provider` mock 实现，便于在无真实校园系统条件下联调。
- 教务读取成功后已接入统一审计记录，后续再演进真实连接器、同步任务与本地快照。

当前进展（`2026-05-11`）：

- 已将 `portal`、`notification`、`analytics` 补齐为可配置的 `memory / postgres` 双后端。
- 已补迁移脚本、`PostgreSQL` 仓储与审计日志持久化实现，门户轮播/公告和通知均带最小种子数据。
- `analytics` 在 `postgres` 模式下可跨重启保留审计日志，后续再补预计算统计与更完整观测。

### 工作流 E：Flutter App 外壳

- 初始化 `Flutter`
- 建立 `feature-first + MVVM` 目录
- 接入路由、状态管理、网络层和本地缓存抽象
- 对齐校园生活主线能力与教务绑定后的受限字段语义

### 工作流 F：质量与运维

- 建立 CI
- 接入 lint / test / format
- 预埋日志、指标、追踪、审计规范

当前进展（`2026-05-10`）：

- 已为 `services/api-server` 增加 Docker 镜像构建入口与 `docker compose` 本地编排。
- 已将 `/readyz` 从静态占位改为真实依赖探测，当前覆盖 `postgres`、`redis`、`object_storage`。
- 已补充 `ops/docker/api-server` 联调文档与环境变量模板，便于团队统一启动后端依赖。

当前进展（`2026-05-10`）：

- 已把 `IAM` 模块切换为可配置的 `memory / postgres_redis` 双后端模式。
- 已落地最小迁移执行器和 IAM 用户表迁移，支持服务启动自动建表。
- 已将用户资料与教务绑定状态落到 `PostgreSQL`，登录会话和验证码落到 `Redis`。
- 已将 `campus_life` 切换为可配置的 `memory / postgres` 双后端，现有二手、跑腿、资料、失物招领读写可持久化到 `PostgreSQL`。

## 里程碑

| 里程碑 | 说明 | 完成标志 |
| --- | --- | --- |
| `M0` | 文档与骨架完成 | 目录、规范、计划、架构文档齐备 |
| `M1` | 基础域可联调 | 认证、权限、门户、通知通路可用 |
| `M2` | 核心业务可试运行 | 跑腿、组局、二手、资料、失物招领具备可演示流程 |

## 风险

- 外部校园系统接口不稳定
- 多端并行开发导致接口漂移
- 权限模型在后期返工成本高
- 小程序和 Flutter 的体验诉求不同，容易错误共享 UI 思路
- 教务绑定状态与联系方式查看权限容易出现前后端语义不一致

## 风险应对

- 所有外部系统先做 provider 封装与 mock
- 契约变更必须走版本和评审
- 权限模型先建资源动作码，再做界面
- 多端共享业务语义，不共享页面实现
- 联系方式字段由后端统一裁剪，多端只消费可见性结果，不自行推断
